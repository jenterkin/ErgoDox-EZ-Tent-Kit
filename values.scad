// keyboard dimensions
inner_width = 190;
inner_height = 133.2;
inner_depth = 25.5;

// board dimensions
depth = 4;
// width and height have a border of 2*depth to allow for walls.
width = inner_width + (depth * 2);
height = inner_height + (depth * 2);

board_bevel_radius = 5;

hinge_offset = 15;

slot_radius = 2.5;
slot_offset = hinge_offset;
